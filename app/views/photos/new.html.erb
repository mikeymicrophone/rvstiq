<%= form_for @photo do |f| %>
  <%= f.file_field :image %>
  <%= f.select :job_id, options_from_collection_for_select(Job.all, :id, :name, @photo.job_id), :show_blank => 'Select a Job' %>
  <%= f.submit %>
<% end %>
